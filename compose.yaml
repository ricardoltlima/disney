version: '3.8'

services:
  my-service:
    image: openjdk:17
    container_name: disney-java-app
    volumes:
      - .:/app
    working_dir: /app
    command: [ "./mvnw", "spring-boot:run" ]
    ports:
      - "8080:8080"
